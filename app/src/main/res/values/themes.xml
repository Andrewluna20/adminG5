<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base SIN action bar (barra negra arriba) -->
    <style name="Theme.TEMAdmin" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/bg_deep</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:navigationBarColor">@color/bg_deep</item>
        <item name="android:windowBackground">@color/bg_deep</item>
        <!-- ★ Forzar sin action bar / sin título -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Tema Splash que hereda de NoActionBar -->
    <style name="Theme.TEMAdmin.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_deep</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.TEMAdmin</item>

        <!-- ★ Aplicar también aquí para que no aparezca durante el splash -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/bg_deep</item>
        <item name="android:navigationBarColor">@color/bg_deep</item>
    </style>
</resources>
